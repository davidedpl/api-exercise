package usersmanagement.rest.v1.assembler;

import com.fasterxml.jackson.databind.JsonNode;
import usersmanagement.domain.UserUpdateHelper;

public class UserUpdateAssembler implements Assembler<UserUpdateHelper> {
    @Override
    public UserUpdateHelper assemble(JsonNode node) {
        return null;
    }
}
